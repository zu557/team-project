// import { Router } from 'express'
// import { getBlogs,getBlog, addBlogs, UpdateBlog, deleteBlog } from '../controllers/blog-conrollers.js'
 
// const router: Router = Router()

// router.get('/blog-post', getBlogs)

// router.get('/blog-post/:id', getBlog)

// router.post('/blog-post', addBlogs)// only for admin

// router.put('/blog-post/:id', UpdateBlog)// only for admin

// router.delete('/delete-blog-post/:id', deleteBlog)// only for admin

// export default router